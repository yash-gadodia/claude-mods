{
  "description": "ui.render on AbovePrompt draws the usage line; the figures arrive on session.measure after each turn and whenever a limit window moves, an in-process push of what the last API response reported; turn.complete adds the turn's own token counts",
  "modules": ["./register.tsx"]
}
