{
  "name": "money-band",
  "version": "0.3.0",
  "description": "A line above the prompt with liquid assets, CPF, debt and month-to-date spend, read from a pair of SQLite databases over ssh.",
  "author": {
    "name": "Yash Gadodia"
  },
  "userConfig": {
    "host": {
      "type": "string",
      "title": "Database host",
      "description": "The ssh alias of the machine holding the SQLite databases. Use `localhost` if they are on this machine.",
      "default": "mini",
      "required": true
    },
    "networthDb": {
      "type": "string",
      "title": "Net-worth database",
      "description": "Path on the host to a SQLite database with `accounts` and `balances` tables.",
      "default": "$HOME/.openclaw/data/networth.db",
      "required": true
    },
    "financeDb": {
      "type": "string",
      "title": "Spending database",
      "description": "Path on the host to a SQLite database with a `transactions` table.",
      "default": "$HOME/.openclaw/data/finance.db",
      "required": true
    }
  },
  "license": "MIT",
  "homepage": "https://github.com/yash-gadodia/claude-mods",
  "repository": "https://github.com/yash-gadodia/claude-mods",
  "keywords": [
    "claude-code",
    "claude-code-plugin",
    "claude-mod",
    "function-hooks",
    "money-band"
  ]
}
