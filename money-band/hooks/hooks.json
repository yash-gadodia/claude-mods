{
  "description": "ui.render on AbovePrompt draws the money line; the figures are fetched from the mini on session start and every 15 minutes, never inside the render hook",
  "modules": ["./register.tsx"]
}
