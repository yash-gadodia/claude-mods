{
  "description": "turn.start resets the count, tool.check on the editing tools counts distinct files and at the threshold asks a Haiku judge, then the user, or denies and ends the turn headless; /scope sets it",
  "modules": ["./register.ts"]
}
